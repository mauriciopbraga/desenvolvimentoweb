const [a, b, c, d] = [1, 2, 3, 4]

// Operadores aritméticos são operadores binários, pois existem dois operandos

const soma = a + b + c + d
const subtracao = a - b
const multiplicacao = c * d
const divisao = c / a
const modulo = a % 2 // Resto da divisão

console.log(soma, subtracao, multiplicacao, divisao, modulo)