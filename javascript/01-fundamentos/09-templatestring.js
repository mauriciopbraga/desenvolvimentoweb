const nome = 'Mauricio'
console.log(`Olá, ${nome}`) // Concatenar sem + ou concat (pode usar quebras de linha)

// Expressão matemática
console.log(`1 + 2 = ${1 + 2}`)

const caps = palavra => palavra.toUpperCase()
console.log(`Testanto ${caps('teste')}!`)